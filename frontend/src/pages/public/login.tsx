import LoginForm from "@/features/auth/components/login-form";
import DoubleColumnLayout from "@/shared/components/double-column-layout";

const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
